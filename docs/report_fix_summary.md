# 実験Ⅳ-3 レポート修正案まとめ

## 修正① Task4の伝搬路推定式を配布資料準拠へ修正

現在：

``` text
h_hat = sum(conj(u_pre_i) u_hat_pre_i) / sum(|u_pre_i|^2)
```

実験テキスト準拠へ変更：

``` text
伝搬路応答は，既知のプリアンブル系列と受信プリアンブル系列の相互相関から推定した。

h_hat = (1/N_pre) Σ u_hat_pre,i × conj(u_pre,i)
```

続けて補足：

``` text
実装では，振幅正規化を含めた形で推定を行った。
```

理由： - 実装式は一般的だが，実験レポートは配布資料準拠が安全。 -
「テキスト準拠＋実装補足」にする。

------------------------------------------------------------------------

## 修正② Task5考察の断定表現を修正

現在：

``` text
品質の低いアンテナをわずかに混ぜることで1ビット誤りが発生した。
```

修正版：

``` text
品質の低いアンテナ系列は重みが小さいものの，雑音成分を含んでいるため，有限長データでは判定境界付近のシンボルへ影響を与えた可能性がある。
```

理由： - 原因を断定しない。 - レポートでは「可能性がある」が安全。

------------------------------------------------------------------------

## 修正③ 最大比合成(MRC)について追記

Task5考察に追加：

``` text
本来は最大比合成（Maximum Ratio Combining：MRC）が理論的に最適とされる。MRCでは伝搬路利得と雑音電力を考慮した重み付けを行う。本実験では簡単化のため，チャネル電力に比例する重みを用いた。
```

理由： - 実験指示に「最適な合成法の検討」がある。 -
理論最適を知ったうえで簡易法を使った説明になる。

------------------------------------------------------------------------

## 修正④ 図番号の変更

図11〜図15などへ変更推奨。

理由： - 元テキスト図と衝突防止。

------------------------------------------------------------------------

## 修正⑤ Task1波形を階段表示

``` python
plt.step(x,y,where='post')
```

理由： - 矩形パルスが自然になる。

------------------------------------------------------------------------

## 修正⑥ Task2コンステレーション改善

``` python
plt.scatter(..., alpha=0.6, s=100)
```

理由： - 理想点との重なりが見やすくなる。

------------------------------------------------------------------------

最優先：①②③
